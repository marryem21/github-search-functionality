import React from "react";

// all the logic and api call will be here

const Repos = () => {
  //display this if no username is searched yet
  return (
    <p>
      1) Insert a valid github user name in the header's search bar, example:
      Marryem21 <br></br> 2) Hold on for a 1 second to get the list of their
      repositories<br></br> 3) Type a repository name and click search
    </p>
  );
};

export default Repos;
